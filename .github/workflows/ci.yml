name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    name: Verify (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.17.0

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22.14.0
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify product
        run: pnpm verify

      - name: Publish suite comparison summary
        if: ${{ !cancelled() && matrix.os == 'ubuntu-latest' }}
        shell: bash
        run: |
          pnpm --silent blueprint:suite-diff \
            examples/core.suite-report.json \
            examples/core-candidate.suite-report.json \
            --format markdown >> "$GITHUB_STEP_SUMMARY"

      - name: Publish declared change fixture summary
        if: ${{ !cancelled() && matrix.os == 'ubuntu-latest' }}
        shell: bash
        run: |
          pnpm --silent blueprint:suite-change \
            examples/core.suite-report.json \
            examples/core-candidate.suite-report.json \
            --plan examples/core.suite-change-plan.json \
            --as-of 2026-08-08 \
            --format markdown >> "$GITHUB_STEP_SUMMARY"
